import { FooterContainer } from "./Footer.styles";

export default function Footer() {
    return (
        <FooterContainer>
            Built with NextJS and TypeScript.
            Somewhat designed on Figma.
        </FooterContainer>
    );
}